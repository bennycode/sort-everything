{
  "configurations": [
    {
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "name": "Run Extension",
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "preLaunchTask": "package",
      "request": "launch",
      "type": "extensionHost"
    }
  ],
  "version": "0.2.0"
}
